name: Release Engine

on:
  push:

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          path: "./engine"
          token: ${{ secrets.GITHUB_TOKEN }}
