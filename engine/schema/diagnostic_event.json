{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DiagnosticEvent",
  "description": "Pubsub wire event for a single `Diagnostic`. Gated by `ENABLE_DIAGNOSTICS` (see above) — construction itself is unconditional, callers decide whether to publish it.",
  "type": "object",
  "required": [
    "category",
    "code",
    "jobId",
    "message",
    "schema",
    "severity",
    "timestamp",
    "workflowId"
  ],
  "properties": {
    "schema": {
      "description": "Wire schema tag for this event shape. Always `\"diagnostic.v1\"`.",
      "type": "string"
    },
    "workflowId": {
      "type": "string",
      "format": "uuid"
    },
    "jobId": {
      "type": "string",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "code": {
      "type": "string"
    },
    "category": {
      "type": "string"
    },
    "severity": {
      "type": "string"
    },
    "effectiveDisposition": {
      "type": [
        "string",
        "null"
      ]
    },
    "nodeId": {
      "type": [
        "string",
        "null"
      ]
    },
    "actionType": {
      "type": [
        "string",
        "null"
      ]
    },
    "featureId": {
      "type": [
        "string",
        "null"
      ],
      "format": "uuid"
    },
    "message": {
      "type": "string"
    },
    "help": {
      "type": [
        "string",
        "null"
      ]
    },
    "sourceSpan": {
      "anyOf": [
        {
          "$ref": "#/definitions/WireSourceSpan"
        },
        {
          "type": "null"
        }
      ]
    },
    "aggregated": {
      "anyOf": [
        {
          "$ref": "#/definitions/WireAggregateInfo"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "WireSourceSpan": {
      "description": "Wire mirror of `reearth_flow_diagnostics::SourceSpan`.",
      "type": "object",
      "required": [
        "offset"
      ],
      "properties": {
        "offset": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "length": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        }
      }
    },
    "WireAggregateInfo": {
      "description": "Wire mirror of `reearth_flow_diagnostics::AggregateInfo`.",
      "type": "object",
      "required": [
        "count",
        "sampleFeatureIds"
      ],
      "properties": {
        "count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "sampleFeatureIds": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid"
          }
        }
      }
    }
  }
}
