{
  "description": "CSV Reader parses the bare 3D WKT (no Z tag) that the CSV Writer emits, then CSV Writer re-emits it unchanged -- the write/read seam that let 3D WKT round-trips break before with_z_tag",
  "workflowPath": "executor-testing/csv-wkt-roundtrip-3d.yml",
  "expectedOutput": {
    "expectedFile": "output.csv"
  }
}
