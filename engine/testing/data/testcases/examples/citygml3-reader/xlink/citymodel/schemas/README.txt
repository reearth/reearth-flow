Placeholder schemas directory for CityGML 3.0 xlink testcase.

This testcase exercises path reconstruction in preprocessing. No schema files are
read by the current reader path, but PLATEAU4.UDXFolderExtractor expects a
schemas archive/directory to exist when materializing the extracted root.
