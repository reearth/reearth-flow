// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: deployment.sql

package gen

import (
	"context"
	"time"
)

const deleteDeployment = `-- name: DeleteDeployment :exec
DELETE FROM deployments WHERE id = $1
`

func (q *Queries) DeleteDeployment(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, deleteDeployment, id)
	return err
}

const getDeployment = `-- name: GetDeployment :one
SELECT id, workspace_id, project_id, workflow_url, description, version, updated_at, head_id, is_head FROM deployments WHERE id = $1
`

func (q *Queries) GetDeployment(ctx context.Context, id string) (Deployment, error) {
	row := q.db.QueryRow(ctx, getDeployment, id)
	var i Deployment
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.ProjectID,
		&i.WorkflowUrl,
		&i.Description,
		&i.Version,
		&i.UpdatedAt,
		&i.HeadID,
		&i.IsHead,
	)
	return i, err
}

const listDeploymentsByIDs = `-- name: ListDeploymentsByIDs :many
SELECT id, workspace_id, project_id, workflow_url, description, version, updated_at, head_id, is_head FROM deployments WHERE id = ANY($1::text[])
`

func (q *Queries) ListDeploymentsByIDs(ctx context.Context, dollar_1 []string) ([]Deployment, error) {
	rows, err := q.db.Query(ctx, listDeploymentsByIDs, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Deployment
	for rows.Next() {
		var i Deployment
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.ProjectID,
			&i.WorkflowUrl,
			&i.Description,
			&i.Version,
			&i.UpdatedAt,
			&i.HeadID,
			&i.IsHead,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertDeployment = `-- name: UpsertDeployment :exec
INSERT INTO deployments (
  id, workspace_id, project_id, workflow_url, description,
  version, updated_at, head_id, is_head
) VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)
ON CONFLICT (id) DO UPDATE SET
  workspace_id = EXCLUDED.workspace_id,
  project_id   = EXCLUDED.project_id,
  workflow_url = EXCLUDED.workflow_url,
  description  = EXCLUDED.description,
  version      = EXCLUDED.version,
  updated_at   = EXCLUDED.updated_at,
  head_id      = EXCLUDED.head_id,
  is_head      = EXCLUDED.is_head
`

type UpsertDeploymentParams struct {
	ID          string
	WorkspaceID string
	ProjectID   *string
	WorkflowUrl string
	Description string
	Version     string
	UpdatedAt   time.Time
	HeadID      *string
	IsHead      bool
}

func (q *Queries) UpsertDeployment(ctx context.Context, arg UpsertDeploymentParams) error {
	_, err := q.db.Exec(ctx, upsertDeployment,
		arg.ID,
		arg.WorkspaceID,
		arg.ProjectID,
		arg.WorkflowUrl,
		arg.Description,
		arg.Version,
		arg.UpdatedAt,
		arg.HeadID,
		arg.IsHead,
	)
	return err
}
