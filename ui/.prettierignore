dist/
build/

node_modules/
.env
.env.*
.env-local

yarn.lock
.yarn/*

src/lib/gql/__gen__
src/routeTree.gen.ts

# Written by the engine's `cargo make schema-feature`. Without this, `yarn
# format:check` fails on it, and a `format:write` would reformat it into a
# permanent diff against the engine's copy under `cargo make check-schema`.
src/lib/intermediateData/feature-intermediate.schema.json

public/mockServiceWorker.js

e2e/playwright-report/
e2e/test-results/
e2e/allure-results/
e2e/allure-report/
e2e/.auth/

CLAUDE.md